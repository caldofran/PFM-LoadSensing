<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.uoc.loadsensing"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="5" />

    <application android:icon="@drawable/icon" android:label="@string/app_name">
 	
 	<uses-library android:name="com.google.android.maps" />
    
        <activity android:name=".SplashActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Login Activity -->
        <activity android:name=".LoginActivity"></activity>
        
        <!-- Tab Menu Activity -->
		<activity android:name=".TabMenuActivity"></activity>
 
        <!--  Map Activity -->
        <activity android:name=".MapNetworkActivity"></activity>
		<activity android:name=".AndroidMapViewActivity"></activity>
 
        <!--  List Network Activity -->
        <activity android:name=".ListNetworkActivity"></activity>
 
        <!--  QR Codes Activity -->
        <activity android:name=".QRCodeActivity"></activity>        
        
        <!-- Single Network Activity -->
        <activity android:name=".SingleNetworkActivity"></activity>

    </application>
    
    <!-- Set permisions -->
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    
</manifest>